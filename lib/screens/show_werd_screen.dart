import 'package:flutter/material.dart';

class ShowWerdScreen extends StatelessWidget {
  const ShowWerdScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}